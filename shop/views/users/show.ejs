<h1 class="center">ユーザー情報
  <%= user.fullName %>
</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <td>
      <%= user.fullName %>
    </td>
  </tr>
  <tr>
    <th>Email</th>
    <td>
      <%= user.email%>
    </td>
  </tr>
  <tr>
    <th>Zip Code</th>
    <td>
      <%= user.zipCode%>
    </td>
  </tr>
  <tr>
    <th>Password</th>
    <td>********</td> <%# 表示しているように見せているだけ、実体は無いのでコピペしても無駄 %> 
  </tr>
</table>

<% if(user.subscribedAccount !== undefined) { %>
<h4 class="center"> This user has a
  <a href="<%=`/subscribers/${user.subscribedAccount}`%>">
    subscribed account </a>.
</h4>
<% } %>
<div class="center">
  <a class="button" href="<%=`/users/${user._id}/edit`%>">
    ユーザーの詳細を編集する
  </a>
</div>
<div class="center">
  <a class="button" href="<%= `/users/${user._id}/delete?_method=DELETE` %>" onclick="return confirm('Are you sure you want to delete this record?')" a>削除</a>
</div>
<div class="center">
  <a class="button" href="/users">全てのユーザー</a>
</div>